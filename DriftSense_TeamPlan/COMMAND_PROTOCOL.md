# COMMAND PROTOCOL

Before coding:
`git status`
`git branch --show-current`

Benchmark against the immutable main dataset:
`data/phase2_dev/pairs.csv`

Never run a generator that writes into `data/phase2_dev/`.

Before handoff:
`git diff --stat`
`git status`

Record every changed file and the exact reproduction command.
